import { PerspectiveCamera } from "three";

export class Camera {
    camera: PerspectiveCamera;
    dfRotateX: number;
    dfRotateY: number;
    dfRotateZ: number;
    dfTranslateX: number;
    dfTranslateY: number;
    dfTranslateZ: number;
}